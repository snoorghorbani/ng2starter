import { FormGroup } from "@angular/forms";
import { FieldConfig, Field } from "../../../models";
export declare class DateFormInputControlComponent implements Field {
    config: FieldConfig;
    group: FormGroup;
    constructor();
}
