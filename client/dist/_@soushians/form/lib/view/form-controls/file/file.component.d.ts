import { FormGroup } from "@angular/forms";
import { FieldConfig, Field } from "../../../models";
export declare class FileFormInputControlComponent implements Field {
    config: FieldConfig;
    group: FormGroup;
    constructor();
}
