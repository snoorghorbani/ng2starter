export declare class PageModel {
    _id: string;
    name: string;
    grid: string;
    constructor({ _id, name, grid }?: {
        _id?: string;
        name?: string;
        grid?: string;
    });
}
